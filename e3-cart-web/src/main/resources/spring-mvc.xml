<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">

    <!--SpringMVC只是控制网站跳转逻辑  -->
    <!-- 自动扫描该包，使SpringMVC认为包下用了@controller注解的类是控制器 -->
    <!-- 加载配置文件 -->
    <context:property-placeholder location="classpath:conf/resource.properties" />
    <context:component-scan base-package="cn.yanzhongxin.cart.controler"></context:component-scan>
    <mvc:annotation-driven/>
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--配置登陆的拦截器-->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/> <!--拦截所有的请求-->
            <bean class="cn.yanzhongxin.cart.interceptor.LoginInterceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>
    <!--配置freemarker-->
    <bean id="freemarkerConfig"
          class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <property name="templateLoaderPath" value="/WEB-INF/ftl/" /> <!--模板路径-->
        <property name="defaultEncoding" value="UTF-8" />
    </bean>


    <!-- 配置资源映射 -->
    <!--<mvc:resources location="/css/" mapping="/css/**"/>
    <mvc:resources location="/js/" mapping="/js/**"/>-->
    <!-- 引用dubbo服务 -->
  <dubbo:application name="e3-item-web"/>
    <dubbo:registry protocol="zookeeper" address="192.168.25.128:2181"/>

    <!--引用与商品信息服务的接口服务-->
   <dubbo:reference interface="cn.yanzhongxin.service.ItemService" id="itemService"/>

    <!--引用单点登陆接口服务,用来获得用户的session，判断是否登陆-->
    <dubbo:reference interface="cn.yanzhongxin.sso.service.TokenService" id="tokenService"/>

    <!--调用购物车的服务-->
    <dubbo:reference interface="cn.yanzhongxin.cart.service.CartService" id="cartService"/>
</beans>